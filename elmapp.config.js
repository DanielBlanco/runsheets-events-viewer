
module.exports = {
  "homepage": "https://DanielBlanco.github.io/runsheets-events-viewer",
  configureWebpack: (config, env) => {
    // Manipulate the config object and return it.
    return config;
  }
}
